##pinyin
###转换汉字为拼音首字母，如 张三-> ZS,ZhangSan

>// 提取拼音, 返回首字母大写形式
>		getFullChars: function(str){	
>		}
		
>// 提取首字母，返回大写形式		
>		getCamelChars: function(str){
>		}

用法
><script src="...[your directory]/pinyin.js"></script>
>pinyin.getCamelChars("张三");
>pinyin.getFullChars("张三");